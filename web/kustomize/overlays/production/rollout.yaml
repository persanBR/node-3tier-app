apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: node-3tier-app2-web
  labels:
    app: node-3tier-app2-web
spec:
  selector:
    matchLabels:
      app: node-3tier-app2-web
  template:
    metadata:
      labels:
        app: node-3tier-app2-web
        istio-injection: enabled
    spec:
      containers:
      - name: node-3tier-app2-web
        image: gbulanov/node-3tier-app2-web
        ports:
        - containerPort: 8080
        env:
          - name: PORT
            value: "8080"
          - name: API_HOST
            value: "http://node-3tier-app2-api:8080"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
          requests:
            cpu: 80m
            memory: 128Mi
  strategy:
    canary:
      canaryService: node-3tier-app2-web-canary
      stableService: node-3tier-app2-web
      trafficRouting:
        istio:
          virtualService:
            name: node-3tier-app2-web
            routes:
            - primary
      steps:
      - setWeight: 10
      - pause: {duration: 2m}
      - setWeight: 30
      - pause: {duration: 30s}
      - setWeight: 50
      - pause: {duration: 30s}
      analysis:
        templates:
        - templateName: node-3tier-app2-web
        startingStep: 2
        args:
        - name: service-name
          value: "node-3tier-app2-web-canary.production.svc.cluster.local"

---

apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: node-3tier-app2-web
spec:
  args:
  - name: service-name
  metrics:
  - name: success-rate
    interval: 10s
    successCondition: result[0] >= 0.8
    failureCondition: result[0] < 0.8
    failureLimit: 3
    provider:
      prometheus:
        address: http://prometheus-server.monitoring
        query: |
          sum(irate(
            istio_requests_total{  
              reporter="source",
              destination_service=~"{{args.service-name}}",
              response_code=~"2.*"
            }[2m]
          )) / sum(irate(
            istio_requests_total{
              reporter="source",
              destination_service=~"{{args.service-name}}"
            }[2m]
          ))
  - name: avg-req-duration
    interval: 10s
    successCondition: result[0] <= 1000
    failureCondition: result[0] > 1000
    failureLimit: 3
    provider:
      prometheus:
        address: http://prometheus-server.monitoring
        query: |
          sum(irate(
            istio_request_duration_milliseconds_sum{
              reporter="source",
              destination_service=~"{{args.service-name}}"
            }[2m]
          )) / sum(irate(
            istio_request_duration_milliseconds_count{
              reporter="source",
              destination_service=~"{{args.service-name}}"
            }[2m]
          ))
      
---

apiVersion: v1
kind: Service
metadata:
  name: node-3tier-app2-web-canary
  labels:
    app: node-3tier-app2-web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: node-3tier-app2-web
